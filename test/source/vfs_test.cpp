#include <vfs/vfs.hpp>

#include <string>

#include <catch2/catch.hpp>

TEST_CASE("Name is vfs", "[library]") {
	// auto const exported = vfs::ExportedClass{};
	// REQUIRE(std::string("vfs") == exported.GetName());
}
